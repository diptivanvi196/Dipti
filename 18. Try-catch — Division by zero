void main() {
  int rollNumber = 25; // Replace with your roll number

  try {
    int result = rollNumber ~/ 0;
    print(result);
  } catch (e) {
    print("Error: Cannot divide roll number $rollNumber by zero.");
  }
}
